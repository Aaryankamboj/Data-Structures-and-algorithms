#include<bits/stdc++.h>
using namespace std;
int main()
{

    string s1="nincompoop";
    // cout<<s1;

    // s1.clear();

    // To check whether the string is empty or not
    // if(!s1.empty()){
    //     cout<<"string is empty";
    // }

    // s1.erase(index_where_to_start_delete, no of elements to delete) 
    // s1.erase(3,3);

    // It returns the first index of substring
    // cout<<s1.find("com");

    // Insert the substring into string 
    // Syntax: s1.insert(index_from_where_to_insert, string to be insert) 
    // s1.insert(2,"lol");
    // cout<<s1;

    // cout<<s1.length();

    // for (int i = 0; i < s1.size(); i++)
    // {
    //     /* code */
    //     cout<<s1[i]<<" ";
    // }


    // To retrieve a substring from a string
    // Syntax: s1.substr(starting_index, no of characters)
    // cout<<s1.substr(6,4);

    string s="786";
    // To convert string into integer stoi is used
    // int x = stoi(s);
    // cout<<x+2;

    // To convert integer to string to_string is used
    // int x=786;
    // cout<<to_string(x)+"2";
     
    string s3 = "nincompoop";
    // s3.assign("microsoft");
    // cout<<s3;
    // cout<<s3.at(4);
    // cout<<s3.c_str();
    
    cout<<s3.resize(6);











}